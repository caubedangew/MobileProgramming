<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?android:selectableItemBackground"
    android:elevation="2dp"
    android:orientation="horizontal"
    android:padding="10dp">

    <!-- Nhóm thông tin môn học -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical"
        android:padding="8dp">

        <!-- Tên môn học -->
        <TextView
            android:id="@+id/tv_subject_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="4dp"
            android:contentDescription="Tên môn học"
            android:text="@string/tv_subject_name"
            android:textColor="#333333"
            android:textSize="16sp"
            android:ellipsize="end"
            android:maxLines="1" />

        <!-- Số tín chỉ -->
        <TextView
            android:id="@+id/tv_credit_number"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="4dp"
            android:contentDescription="Số tín chỉ"
            android:text="@string/tv_credit_number"
            android:textColor="#333333"
            android:textSize="16sp"
            android:ellipsize="end"
            android:maxLines="1" />
    </LinearLayout>

    <!-- Biểu tượng xóa -->
    <ImageView
        android:id="@+id/img_delete_subject"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_gravity="center"
        android:contentDescription="Xóa môn học"
        android:padding="5dp"
        android:clickable="true"
        android:focusable="true"
        android:foreground="?attr/selectableItemBackgroundBorderless"
        android:src="@drawable/ic_delete" />
</LinearLayout>